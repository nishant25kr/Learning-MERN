<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Person Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        .table {
            background-color: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        th, td {
            text-align: center;
        }

        a {
            text-decoration: none;
        }

        .btn-custom {
            padding: 5px 10px;
            border-radius: 5px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="card p-4">
            <h2 class="text-center mb-4">Details of Persons</h2>
            <table class="table table-bordered table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Place</th>
                        <th>Age</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let items of result){ %>
                        <tr>
                            <td><%= items.name %></td>
                            <td><%= items.place %></td>
                            <td><%= items.age %></td>
                            <td>
                                <a href="/edit/<%=items._id%>" class="btn btn-warning btn-sm btn-custom">Edit</a>
                                <a href="/delete/<%=items._id%>" class="btn btn-danger btn-sm btn-custom">Delete</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <div class="text-center mt-3">
                <a href="/" class="btn btn-primary">Add More</a>
            </div>
        </div>
    </div>
</body>
</html>
